'use strict';

describe('myApp.browsedb module', function() {

  beforeEach(module('myApp.browsedb'));

  describe('browsedb controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var browsedbCtrl = $controller('BrowseDbCtrl');
      expect(browsedbCtrl).toBeDefined();
    }));

  });
});
